import * as React from 'react';

import Options from './Options';

import './index.styl';

export default function Synchronize() {
  return (
    <div className="tcfSynchronize">
      <Options />
    </div>
  );
}
